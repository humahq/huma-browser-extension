{
    "name": "huma-chrome-plugin",
    "version": "1.0.0",
    "scripts": {
      "build": "mkdir -p dist && cp -r background.js content.js browser-polyfill.js manifest.json icons dist/"
    }
  }
  